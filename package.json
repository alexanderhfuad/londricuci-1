{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cd artifacts/cuci-cuci && npm install && npm run build && cp -r dist ../../../public_html",
    "typecheck": "cd artifacts/cuci-cuci && npm run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
